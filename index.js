import React from "react";
import ReactDOM from "react-dom";
import PopulationCharts from "./App";

ReactDOM.render(
  <React.StrictMode>
    <PopulationCharts />
  </React.StrictMode>,
  document.getElementById("root")
);